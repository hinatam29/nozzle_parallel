# 変更履歴（1ノズル → 2ノズル並列化・可視化・高速化）

このドキュメントは、元の単一ノズル版に対して行った変更をまとめたものです。
元の6つの物理ソースは `_backup_1nozzle/` に保存してあります
（`diff _backup_1nozzle/geo.f geo.f` などで差分を確認できます）。

---

## 1. 2本ノズルの並列配置

軸対称（円筒 r-z）ソルバはそのままに、左端（対称軸 x=0）の既存ノズルに加え、
右端（鏡面 x=xlen）に鏡像の半ノズルを追加。ピッチは領域幅 10cm。

- **subcom.inc** : 共通ブロック `/arry0/` に指標 `inoz2, ihol2` を追加。
- **geo.f** : 2本目ノズルの内側端 `inoz2 = nx - inoz`、対向電極の右穴
  `ihol2 = nx - ihol` を計算・出力。
- **potential.f** : 右端の半ノズル（電圧 `vanode`）の電位境界条件ブロックを追加
  （do 721 / 731 / 741）。対向電極を左右対称に穴あけ（範囲を `ihol..ihol2` に、
  右側面 do 761 を追加）。
- **initial.f** : パーティクルを2ノズルに交互射出（奇数 k は左 x=0、偶数 k は右 x=xlen）。
- **pcount.f** : 右端 x=xlen をパーティクル除去から鏡面反射に変更（左軸と同じ速度反転）。
- **eforce.f** : 右端付近で場の参照セル `inex` が未設定になるのを防ぐ安全初期化を追加。
- **intraction.f** : 右鏡面 x=xlen に対するクーロン鏡像項を追加（既存の左軸鏡像と対称）。

## 2. 可視化のためのデータ出力（main.f）

- 噴霧粒子データ `spray.dat` を出力（xp, yp, dp, clt, pout）。
- 場データ `flow.dat` に吸収濃度 `cgg` の列を追加（変数 = x, y, phi, cvolume, cgg）。
- 各出力ごとに `flush` を追加。途中で `Ctrl+C` 停止しても完成フレームがディスクに残る。

## 3. 可視化プログラム（visualize.py 新規）

- `spray.dat`（噴霧粒子）を読み、静止画 PNG とアニメーション（MP4／GIF）を生成。
- 2 通りの見方を出力する:
  - **広がり (spread)** : 液滴位置（両ノズルからの噴霧の広がり）。
  - **吸収 (absorb)** : 液滴を吸収量 `clt` で色分け（対数スケール）。
    ノズル直下は暗く、落下して吸収が進むほど明るくなる。
- 液滴が固まって見えないため、分布範囲に自動ズーム。粒径 `dp` で大きさ付け。
- Fortran の3桁指数（E 省略、例 `0.35212361-147`）と `NaN` を正しく読むよう対応。
- 気相の吸収場 `cgg` ヒートマップは（要望により）表示しない。
- 粒子は istp=18000 で発散するので、`--max-frames 18` で正常範囲のみ扱える。

## 4. ソルバの高速化（potential.f）

- BiCGSTAB の収束判定 `EPS` を `1.0E-40`（倍精度で到達不能）→ `1.0E-6` に変更。
  変更前は毎ステップ上限 10000 反復を回し切っており、これが計算が極端に遅い主因だった。
  結果はほぼ不変で、大幅に高速化。

## 5. ビルド設定（makefile）

- コンパイルフラグに `-std=legacy -fallow-argument-mismatch` を追加。
  新しい gfortran で、旧コードの引数不一致（例 advVOF.f）をエラーではなく警告に。

## 6. 座標系（円筒 2次元軸対称のまま／一度平面化したが元に戻した）

左右非対称で良いとの方針のため、**円筒座標（2次元軸対称）を維持**する。
ノズル1は軸上(r=0)に丸ごと、ノズル2は右端(r=xlen)で半分が本物・半分が鏡面。

一時的に平面2次元化（1/r 除去）したが、以下を円筒に戻した:
- **potential.f** : 1/r 係数 `rrkps=1./xx2`、半径流束の r 重み
  `diel=0.5*(x(i,k)+x(i+1,k))*eps` を復活。
- **diffusion1.f** : セル体積を円筒 `vcell=2πx*sdx*sdy`（任意格子で正しい形）、
  気相拡散の円筒第1次微分項 `(1/sdx)*(cg8-cg0)/sdx` を復活。

## 8. ノズル間隔 4cm

- 領域幅 `xlen` を 10cm → 4cm に統一（geo.f, initial.f, intraction.f, pcount.f）。
  円筒座標ではノズル2は半径 4cm のリング（半分が本物・半分が鏡面）。
- 液滴間クーロン反発は intraction.f の直接和が全ペアを合計しており、
  ノズル間の相互作用も含む（間隔を縮めると距離が近くなり自然に強くなる）。

## 7. 粒子発散対策（viscous.f）

- 抗力係数 `Cdf = 24/Rep` が粒子速度→0 で発散(Inf/NaN)し、1粒子でも NaN になると
  粒子間クーロン力の総和で全粒子に伝播していた（istp≈18000 で一斉 NaN 化）。
  `Rep` に下限 1e-8 を設けて回避。

---

## 既知の課題

- 平面2次元化・発散対策の効果は再計算して確認する必要がある
  （左右対称になるか、istp=18000 の先まで伸びるか）。
